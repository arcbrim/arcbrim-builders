---
import BaseLayout from "../layouts/BaseLayout.astro";
import { loadPage } from "../lib/pageContent";

const { data, html } = loadPage("approach");
---
<BaseLayout title={`${data.title ?? "Approach"} | ARCBRIM BUILDERS`} current="approach">
  <section class="section">
    <div class="stack">
      <h1>{data.title ?? "Approach"}</h1>
      <p class="lede">{data.lede ?? ""}</p>

      <div class="callout">
        <div class="prose" set:html={html}></div>
      
        <div class="actions">
          <a class="cta" href="/inquiry">Begin an inquiry</a>
        </div>
      </div>  
    </div>
  </section>
</BaseLayout>
 