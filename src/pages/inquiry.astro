---
import BaseLayout from "../layouts/BaseLayout.astro";
import { loadPage } from "../lib/pageContent";

const { data, html } = loadPage("inquiry");
---
<BaseLayout title={`${data.title ?? "Inquiry"} | ARCBRIM BUILDERS`} current="inquiry">
  <section class="section">
    <div class="stack">
      <h1>{data.title ?? "Inquiry"}</h1>
      <p class="lede">{data.lede ?? ""}</p>

      <div class="callout">
        <div class="prose" set:html={html}></div>
      </div>

      <div class="callout">
        <form class="inquiry-form" action="https://api.web3forms.com/submit" method="POST">
          <input type="hidden" name="access_key" value="fe608e07-4c58-4921-a82e-14fd92975503" />
          <input type="hidden" name="subject" value="New Inquiry — Arcbrim Builders" />
          <input type="hidden" name="redirect" value="https://arcbrimbuilders.com" />
          <input type="checkbox" name="botcheck" style="display:none;" />

          <div class="field">
            <label for="name">Name</label>
            <input id="name" name="name" type="text" required />
          </div>

          <div class="field">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" required />
          </div>

          <div class="field">
            <label for="message">Project overview</label>
            <textarea id="message" name="message" rows="6" required></textarea>
          </div>

          <div class="actions">
            <button type="submit" class="cta">Submit inquiry</button>
          </div>
        </form>
      </div>
    </div>
  </section>
</BaseLayout>